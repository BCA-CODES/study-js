const car = {
    name:"tesla",
    type:"electric",
    price: "$20000"
}

for (const key in car) {
    // console.log(`${key} is ${car[key]}`);
    
}

const truck = ["tata","ashok leyland"]
for (const key in truck) {
    console.log(`${truck[key]}`);
    
}

